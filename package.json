{
  "name": "school-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "generate-vapid-keys": "node scripts/generate-vapid-keys.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
